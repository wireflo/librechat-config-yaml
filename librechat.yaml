version: 1.1.6

endpoints:
  openAI:
    streamRate: 25
  anthropic:
    streamRate: 25
 
  # the `all` setting would override all the above values, making them unnecessary to be set
  all:
    streamRate: 25
